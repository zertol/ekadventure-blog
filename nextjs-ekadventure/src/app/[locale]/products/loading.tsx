import LoadingSpinner from "@/components/UI/Common/LoadingSpinner/page";
import React from "react";

const Loading: React.FC = () => {
  return (
   <LoadingSpinner />
  );
};

export default Loading;